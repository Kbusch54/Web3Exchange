import React from 'react'

interface Props {
    maxValue: number;
    minVlaue: number;
    value: number;
    divisible: number;
    stringValue: string;
}

const loanPoolProposalForm: React.FC<Props> = ({maxValue,divisible,minVlaue,stringValue,value }) => {
    return (
        <div>
            
        </div>
    )
}

export default loanPoolProposalForm
